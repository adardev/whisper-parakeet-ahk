<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Health Connect: lectura de todos los tipos de datos de salud -->
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_ACTIVE_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_TOTAL_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_SLEEP" />
    <uses-permission android:name="android.permission.health.READ_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_HEIGHT" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_PRESSURE" />
    <uses-permission android:name="android.permission.health.READ_BLOOD_GLUCOSE" />
    <uses-permission android:name="android.permission.health.READ_OXYGEN_SATURATION" />
    <uses-permission android:name="android.permission.health.READ_BODY_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_HYDRATION" />
    <uses-permission android:name="android.permission.health.READ_MENSTRUATION" />
    <uses-permission android:name="android.permission.health.READ_POWER" />
    <uses-permission android:name="android.permission.health.READ_SPEED" />
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />
    <uses-permission android:name="android.permission.health.READ_RESPIRATORY_RATE" />
    <uses-permission android:name="android.permission.health.READ_SKIN_TEMPERATURE" />
    <uses-permission android:name="android.permission.health.READ_CERVICAL_MUCUS" />
    <uses-permission android:name="android.permission.health.READ_VO2_MAX" />
    <uses-permission android:name="android.permission.health.READ_BODY_FAT" />
    <uses-permission android:name="android.permission.health.READ_BONE_MASS" />
    <uses-permission android:name="android.permission.health.READ_LEAN_BODY_MASS" />
    <uses-permission android:name="android.permission.health.READ_BASAL_METABOLIC_RATE" />
    <uses-permission android:name="android.permission.health.READ_RESTING_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE_VARIABILITY" />
    <uses-permission android:name="android.permission.health.READ_STEPS_CADENCE" />
    <uses-permission android:name="android.permission.health.READ_FLOORS_CLIMBED" />
    <uses-permission android:name="android.permission.health.READ_WHEELCHAIR_PUSHES" />
    <uses-permission android:name="android.permission.health.READ_INTERMITTENT_SPO2" />
    <uses-permission android:name="android.permission.health.READ_MINDFULNESS" />
    <uses-permission android:name="android.permission.health.READ_SLEEP_STAGE" />
    <uses-permission android:name="android.permission.health.READ_PLANNED_EXERCISE" />
    <uses-permission android:name="android.permission.health.READ_CERVICAL_MUCUS_INSIGHT" />
    <uses-permission android:name="android.permission.health.READ_VO2_MAX_AVERAGE" />

    <!-- Write permissions (needed for the app to be listed in Health Connect) -->
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_DISTANCE" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_SLEEP" />
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE" />
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT" />
    <uses-permission android:name="android.permission.health.WRITE_HEIGHT" />

    <!-- Health Connect companion -->
    <uses-permission android:name="android.permission.health.HEALTH_CONNECT_COMPANION" />

    <!-- Health Connect query: permite que Health Connect descubra esta app -->
    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <package android:name="com.google.android.healthconnect.controller" />
    </queries>

    <!-- Android 11+ package visibility -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".NemotronApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.NemotronVoiceIME">

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- ============================================================== -->
        <!--  MainActivity: TOGGLE INVISIBLE (es el LAUNCHER por defecto).  -->
        <!-- ============================================================== -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.NemotronVoiceIME.Transparent"
            android:taskAffinity="" />

        <!-- ============================================================== -->
        <!--  SetupActivity: wizard de configuracion.                       -->
        <!-- ============================================================== -->
        <activity-alias
            android:name=".SetupAliasV2"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Nemotron"
            android:targetActivity=".ui.SetupActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity
            android:name=".ui.SetupActivity"
            android:exported="false"
            android:label="Nemotron" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/settings_title"
            android:parentActivityName=".ui.SetupActivity" />

        <!-- ============================================================== -->
        <!--  AppPickerActivity: seleccionar apps a congelar (freeze).       -->
        <!--  Tambien es target del long-press en el QS tile.              -->
        <!-- ============================================================== -->
        <activity
            android:name=".dhizuku.AppPickerActivity"
            android:exported="true"
            android:label="Freeze Apps"
            android:theme="@style/Theme.NemotronVoiceIME"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- Long-press en tiles abre la app correspondiente -->
        <activity
            android:name=".dhizuku.TilePreferencesActivity"
            android:exported="true"
            android:theme="@style/Theme.NemotronVoiceIME.Transparent">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>

        <service
            android:name=".guard.DndKeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Foreground service de grabación de voz -->
        <service
            android:name=".record.VoiceRecordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Guard anti-adicción: apaga la pantalla y cierra Instagram al abrir Reels o tras mucho scroll en Inicio, y WhatsApp si ves Status.
             Event-driven (accesibilidad), sin polling ni gasto de batería -->
        <service
            android:name=".guard.AntiScrollAccessibilityService"
            android:exported="false"
            android:label="@string/guard_a11y_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice.AccessibilityServiceInfo"
                android:resource="@xml/accessibility_guard_config" />
        </service>

        <!-- Temp receiver para descongelar apps desde adb -->
        <receiver
            android:name=".dhizuku.UnhideReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.nemotron.voiceime.UNHIDE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".dhizuku.CarConnectionReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_DETACHED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".dhizuku.AutoFreezeBootReceiver"
            android:exported="false"
            android:directBootAware="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- ============================================================== -->
        <!--  Tiles dedicados: congelar/descongelar una app concreta.        -->
        <!-- ============================================================== -->
        <service
            android:name=".dhizuku.AndroidAutoTileService"
            android:exported="true"
            android:icon="@drawable/ic_auto_tile"
            android:label="Auto"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".dhizuku.TelegramTileService"
            android:exported="true"
            android:icon="@drawable/ic_orca_tile"
            android:label="Orca"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".dhizuku.GmsTileService"
            android:exported="true"
            android:icon="@drawable/ic_dolby_tile"
            android:label="Atmos"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".dhizuku.SyncthingTileService"
            android:exported="true"
            android:icon="@drawable/ic_syncthing_tile"
            android:label="Syncthing"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".dhizuku.SyncthingAdareduTileService"
            android:exported="true"
            android:icon="@drawable/ic_syncthing_tile"
            android:label="Syncthing"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Wallet: tap abre Wallet + NFC on/off sin service en background -->
        <service
            android:name=".dhizuku.WalletTileService"
            android:exported="true"
            android:icon="@drawable/ic_wallet_tile"
            android:label="Wallet"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Galaxy Wearable: congelar/descongelar la app de Samsung -->
        <service
            android:name=".dhizuku.WatchManagerTileService"
            android:exported="true"
            android:icon="@drawable/ic_watch_tile"
            android:label="Galaxy Wearable"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Galaxy Fit3 Plugin: congelar/descongelar el plugin de la pulsera -->
        <service
            android:name=".dhizuku.Fit3TileService"
            android:exported="true"
            android:icon="@drawable/ic_fit3_tile"
            android:label="Fit3"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Work: congelar/descongelar apps de trabajo juntas -->
        <service
            android:name=".dhizuku.WorkTileService"
            android:exported="true"
            android:icon="@drawable/ic_work_tile"
            android:label="Work"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Uber Driver: congelar/descongelar Uber Driver -->
        <service
            android:name=".dhizuku.UberDriverTileService"
            android:exported="true"
            android:icon="@drawable/ic_driver_tile"
            android:label="Driver"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- ============================================================== -->
        <!--  ShortcutActivity: transparente, abre apps desde atajos.       -->
        <!-- ============================================================== -->
        <activity
            android:name=".ui.ShortcutActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.NemotronVoiceIME.Transparent">
        </activity>

        <activity
            android:name=".ui.ShortcutPickerActivity"
            android:exported="false"
            android:label="Atajos de apps" />

        <activity
            android:name=".health.HealthSetupActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.NemotronVoiceIME.Transparent">
            <intent-filter>
                <action android:name="androidx.health.healthconnect.permission.ACTION_HEALTH_CONNECT_PERMISSION" />
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity>

        <!-- ============================================================== -->
        <!--  Health Connect: transferencia de datos de salud al NAS.       -->
        <!-- ============================================================== -->
        <service
            android:name=".health.HealthTransferService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="health_data_transfer" />
        </service>

        <receiver
            android:name=".health.HealthBootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.health.connect.PERMISSIONS_APP_NAME"
            android:value="Nemotron Health" />

    </application>

</manifest>
