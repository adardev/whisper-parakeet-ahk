[Unit]
Description=Virtual keyboard for Handy Separate
After=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/ydotoold --socket-path=%t/ydotool.socket --socket-perm=0600
Restart=on-failure

[Install]
WantedBy=default.target
